{
  "name": "backend-course-2025-7",
  "version": "1.0.0",
  "description": "Inventory service lab",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "debug": "node --inspect=0.0.0.0:9229 server.js",
    
    "docker:build": "docker build -t inventory-service .",
    "docker:run": "docker run -p 3000:3000 --env-file .env inventory-service",

    "docker:dev": "docker compose -f docker-compose.dev.yml up --build",
    "docker:down": "docker compose -f docker-compose.dev.yml down",

    "lint": "echo \"(optional) linter command goes here\"",
    "test": "echo \"(optional) tests run here\""
  },
  "dependencies": {
    "commander": "^9.5.0",
    "dotenv": "^16.6.1",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "mysql2": "^3.11.0",
    "superagent": "^8.1.2",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^5.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.11"
  }
}
